import React from "react";

interface props {}

const Footer: React.FC<props> = () => {
  return (
    <div style={{ position: "fixed", top: "90vh", marginRight: "50%" }}>
      Footer
    </div>
  );
};

export default Footer;
